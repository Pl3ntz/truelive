# Build output — the publishable .zip is generated from source, not committed.
build/
dist/
web-ext-artifacts/

# Node tooling (dev-only; installed for lint/CI and the web-ext Firefox build)
node_modules/

# Marketing/store assets — não versionados (gerados a partir dos HTML localmente).
publishing/assets/

# Credenciais da API de publicação da Chrome Web Store (scripts/publish-cws.mjs).
# NUNCA versionar: o refresh_token publica em nome do Owner sem senha nem 2FA.
.env
.env.local

# OS / editor / cloud cruft
.DS_Store
Thumbs.db
desktop.ini
*.log
